export const defaultLabelOffset = {x: 5, y: -10};

export interface Offsets {
    custom: number;
    product: number;
    commodity: number;
}

export const EvoOffsets: Offsets = {
    custom: 3.5,
    product: 8,
    commodity: 14,
};
